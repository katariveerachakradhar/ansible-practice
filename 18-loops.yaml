- name: install packages
  hosts: frontend
  become: yes
  tasks:
  - name: installting packages
    ansible.builtin.dnf:
      name: "Packages are, {{ item }}"
      state: installed
    loop:
    - mysql
    - nginx
    - python3
    