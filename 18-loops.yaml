- name: install packages
  hosts: frontend
  become: yes
  tasks:
  - name: installting packages
    ansible.builtin.package:
      msg: "Packages are, {{ item }}"
    loop:
    - mysql
    - nginx
    - python3
    